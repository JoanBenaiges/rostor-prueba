const Contact = ({ language }) => {

    return (
        <>
            {language === 'en' ? (
                <>
                    <h2>first color</h2>


                </>
            ) : (
                <>
                    <h2>primer color</h2>


                </>
            )}

        </>
    )
}

export default Contact